# <领域名称> Validation

## 1. 验证目标

- 证明业务能力由正确领域拥有。
- 证明跨域调用只依赖公开契约。
- 证明数据与事务边界符合 Spec。
- 证明公开契约和核心逻辑具备完整、准确的中文 Javadoc。

## 2. 结构验证

| 检查项 | 验证方式 | 通过标准 | 失败证据 |
| --- | --- | --- | --- |
| `@ApplicationModule` 声明 | 结构测试/源码审计 | 每个业务领域具有根包声明 |  |
| 允许依赖 | `ApplicationModules.verify()` | 仅出现 Spec 中允许的依赖 |  |
| 内部包访问 | 结构测试/静态审计 | 无跨域访问内部包 |  |
| 数据所有权 | SQL/Dao/Repository 检索 | 每张业务表只有一个写入领域 |  |
| 循环依赖 | 结构测试 | 无循环或已记录临时迁移豁免 |  |

结构测试命令：

```bash
<填写目标项目验证命令>
```

## 3. Javadoc 验证

```bash
python3 <modulith-skill-root>/scripts/audit_java_modulith.py \
  --repo-root <project-root> \
  --base-package <base-package> \
  --fail-on warning
```

| 检查项 | 通过标准 | 人工复核 |
| --- | --- | --- |
| 公开类型与 API | 无缺失 Javadoc | 语义、调用约束和兼容承诺准确 |
| POJO 与字段 | 类型、字段/record component 有说明 | 单位、空值、状态、ID 口径准确 |
| 方法参数与返回值 | `@param`、非 void 的 `@return` 完整 | 未复述名称，失败语义准确 |
| 核心方法 | 有目标、不变量和相关事务/幂等说明 | 与实现和测试一致 |
| Javadoc 构建 | 目标项目 Javadoc 命令通过 | 无无效链接和告警 |

Javadoc 构建命令：

```bash
<填写目标项目验证命令>
```

## 4. 行为验证

| 场景 | 前置数据 | 操作 | 预期业务结果 | 预期事件/副作用 |
| --- | --- | --- | --- | --- |
| 主路径 |  |  |  |  |
| 重复请求/幂等 |  |  |  |  |
| 业务拒绝 |  |  |  |  |
| 部分失败 |  |  |  |  |
| 事件重试/补偿 |  |  |  |  |

## 5. 运行态回证

- 环境与版本：
- 请求/任务/事件标识：
- 日志与指标：
- 数据记录：
- 实际触达的外部系统：
- 未执行的写操作或范围边界：

## 6. 失败判定与回滚

- 失败判定：
- 停止条件：
- 回滚步骤：
- 存量数据/消息处理：
