interface:
  display_name: "Modulith"
  short_description: "业务领域识别、Modulith 映射与演进验证"
  default_prompt: "使用 $modulith 基于当前代码和业务事实规划服务端领域，输出业务领域 DAG、能力与数据所有权、Modulith 映射、迁移步骤和验证方案。"
policy:
  allow_implicit_invocation: true
